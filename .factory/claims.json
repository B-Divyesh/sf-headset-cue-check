[
  {
    "id": "guided-check",
    "claim": "Runs six headset cue observations and creates a local setup card",
    "where": "landing page and README",
    "test": "npm run test:e2e -- --grep @claim:guided-check",
    "sandbox": "fresh browser context at /demo; complete all six ratings; save settings; assert six results and a saved card"
  },
  {
    "id": "demo-isolation",
    "claim": "The one-click demo uses sample data kept separate from real setup cards",
    "where": "landing action, demo banner, README, .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; enter from the first screen; remove and reset sample data; exit to the real app; assert no real cards"
  },
  {
    "id": "local-privacy",
    "claim": "Notes and setup cards stay in the browser with no accounts, analytics, ads, tracking, or remote storage",
    "where": "landing facts, footer, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-privacy",
    "sandbox": "fresh /demo context; complete the full workflow while recording every request; assert same-origin GET only, no cookies, and no web storage"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh /demo context; wait for service-worker control; set the browser offline; reload; start the sample check; play bundled speech to completion"
  },
  {
    "id": "free-use",
    "claim": "Free to use",
    "where": "landing facts and README",
    "test": "npm run test:e2e -- --grep @claim:free-use",
    "sandbox": "fresh context; assert the free fact and absence of checkout, billing, login, or signup paths"
  },
  {
    "id": "json-portability",
    "claim": "Exports and imports setup cards as JSON",
    "where": "saved-card controls, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:json-portability",
    "sandbox": "fresh /demo context; download and inspect the real export; import a valid changed card; assert it appears"
  },
  {
    "id": "copy-print",
    "claim": "Copies and prints a setup-card summary",
    "where": "setup-card controls and README",
    "test": "npm run test:e2e -- --grep @claim:copy-print",
    "sandbox": "fresh /demo context; open the sample card; assert clipboard text and that the print action invokes browser printing"
  },
  {
    "id": "remove-undo",
    "claim": "Removes a setup card with immediate undo",
    "where": "saved-card controls and README",
    "test": "npm run test:e2e -- --grep @claim:remove-undo",
    "sandbox": "fresh /demo context; confirm removal; assert card disappears; activate Undo; assert it returns"
  },
  {
    "id": "keyboard-access",
    "claim": "The complete interface is keyboard operable with live status regions, visible focus, and touch targets at least 44 pixels high",
    "where": "README and interface",
    "test": "npm run test:e2e -- --grep @claim:keyboard-access",
    "sandbox": "fresh /demo context; tab to Import JSON; assert live regions, visible focus, and target size; operate the check with keyboard; measure the legal return link"
  },
  {
    "id": "non-diagnostic-results",
    "claim": "The check records personal observations; it does not diagnose hearing, produce pass or fail results, or certify hardware",
    "where": "landing limit, saved setup card, terms, and README",
    "test": "npm run test:e2e -- --grep @claim:non-diagnostic-results",
    "sandbox": "fresh /demo context; verify the pre-check limits; open the populated sample card; assert six observations, the hardware and hearing limits, and no pass or fail result"
  },
  {
    "id": "manual-routing",
    "claim": "Audio follows the operating-system output; the app cannot identify that device or change or verify its settings",
    "where": "landing limit, setup form, saved setup card, terms, and README",
    "test": "npm run test:e2e -- --grep @claim:manual-routing",
    "sandbox": "fresh /demo context; verify the routing limit; complete the observations; assert the headset name starts blank; enter distinct settings and assert the card records those user-entered values"
  }
]
